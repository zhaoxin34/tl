#!/usr/bin/env python
# -*- coding: utf-8 -*-

from tornado.options import define, options
import pymongo

# client = pymongo.MongoClient("mongodb://" + options.mongodb_host + ":" + options.mongodb_port)
